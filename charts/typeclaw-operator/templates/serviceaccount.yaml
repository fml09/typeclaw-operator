{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "typeclaw-operator.serviceAccountName" . }}
  labels:
    {{- include "typeclaw-operator.labels" . | nindent 4 }}
automountServiceAccountToken: true
{{- end }}
