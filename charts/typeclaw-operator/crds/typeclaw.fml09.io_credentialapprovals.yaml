---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: credentialapprovals.typeclaw.fml09.io
spec:
  group: typeclaw.fml09.io
  names:
    categories:
    - typeclaw
    kind: CredentialApproval
    listKind: CredentialApprovalList
    plural: credentialapprovals
    singular: credentialapproval
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.requestName
      name: Request
      type: string
    - jsonPath: .spec.decision
      name: Decision
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CredentialApprovalSpec is a separate append-only administrator decision. The
              request controller never accepts approval through CredentialRequest.status.
            properties:
              decision:
                description: |-
                  CredentialDecision is the only decision an administrator may append through
                  a CredentialApproval. The Kubernetes API audit record supplies identity.
                enum:
                - Approve
                - Deny
                type: string
              requestName:
                description: RequestName is the exact namespaced CredentialRequest
                  being decided.
                minLength: 1
                type: string
            required:
            - decision
            - requestName
            type: object
            x-kubernetes-validations:
            - message: credential approval is immutable
              rule: self == oldSelf
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
